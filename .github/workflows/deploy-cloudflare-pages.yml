name: Deploy Cloudflare Pages

on:
  push:
    branches:
      - desktop-web
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  # A push to desktop-web deploys straight to the live web app, and PR checks
  # cannot gate a direct push. Verify here so the deploy job never runs against
  # a tree that fails typecheck or tests.
  verify:
    name: Typecheck and tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run api:prisma:generate

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm test -- --run

  deploy-desktop-web:
    name: Deploy Desktop Web App (Cloudflare Pages)
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run api:prisma:generate

      - name: Build Expo Web Bundle
        run: npx expo export --platform web

      - name: Configure Cloudflare Pages SPA Redirects
        run: echo "/*    /index.html   200" > dist/_redirects

      - name: Deploy Expo Web App to Cloudflare Pages
        run: npx -y wrangler@3 pages deploy dist --project-name venue-wrangler --branch desktop-web --commit-hash ${{ github.sha }} --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
